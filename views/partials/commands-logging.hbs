<h4 id="logging" class="text-center">Logging Seeings Commands</h4>
<div class="permissions text-center">{User required to have "Manage Permissions"}</div>
<table class="commands table table-striped table-hover">
  <thead>
    <tr>
      <th class="text-center col-xs-4">Command</th>
      <th class="text-center col-xs-4">Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>set ban log &lt;channel id&gt;</td>
      <td>Set the ban log for the server.<br /> If no ID is provided, or the id is invalid, the log setting is removed, meaning you won't get logs. <br /> - <span class="example">Example <code>set ban log 440564083696336897</code><span></td>
    </tr>
    <tr>
      <td>set unban log &lt;channel id&gt;</td>
      <td>Set the unban log for the server.<br /> If no ID is provided, or the id is invalid, the log setting is removed, meaning you won't get logs. <br /> - <span class="example">Example <code>set unban log 440564083696336897</code><span></td>
    </tr>
    <tr>
      <td>set member remove log &lt;channel id&gt;</td>
      <td>Set the log for members being kicked and leaving for the server.<br /> If no ID is provided, or the id is invalid, the log setting is removed, meaning you won't get logs. <br /> - <span class="example">Example <code>set member remove log 440564083696336897</code><span></td>
    </tr>
    <tr>
      <td>set message delete log &lt;channel id&gt;</td>
      <td>Set the message deletion log for the server.<br /> If no ID is provided, or the id is invalid, the log setting is removed, meaning you won't get logs.<br /> - <span class="example">Example <code>set message delete log 440564083696336897</code><span></td>
    </tr>
    <tr>
      <td>set mod &lt;role id&gt;</td>
      <td>Set the mod role for the server.<br /> If no ID is provided, or the id is invalid, the mod setting is removed, meaning mentions for the vulgar name log won't do anything.. <br /> - <span class="example">Example <code>set mod 77190254141911040</code><span></td>
    </tr>
    <tr>
      <td>set vulgar log &lt;channel id&gt;</td>
      <td>Set the vulgar log for the server. This log will notify moderators when someone with a vulgar name (globally predefined based on an outside list) joins the server.<br /> If no ID is provided, or the id is invalid, the log setting is removed, meaning you won't get logs. <br /> - <span class="example">Example <code>set ban log 440564083696336897</code><span></td>
    </tr>
  </tbody>
</table>
